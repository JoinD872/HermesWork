# 逆向纯净当前状态

> 更新时间：2026-09-06
> 适用范围：`E:\Opencode\MXD\逆向_纯净`

## 当前阶段

当前以离线静态分析、资源核对和证据整理为主。涉及运行中游戏进程、驱动、注入、发包或真实输入的动态验证仍受安全边界和隔离环境条件限制，不能把离线推测写成运行时确认。

## 已确认或可复用

- 已建立 v39 IL2CPP dump、元数据、资源包和历史分析的目录索引。
- 已整理角色、Camera、地图实体、网络分发和部分 NonFinite 相关静态证据。
- NonFinite 单条结论统一使用 `CONFIRMED`、`STRONG_INFERENCE` 和 `UNKNOWN`。
- 原始 dump、metadata、资源包和历史证据保持只读；纠正结论时追加新报告。

## 当前阻塞

- 运行时字段语义、动态参数和服务端协议仍缺少安全、同版本的运行时证据。
- 需要隔离环境的 Frida、驱动、注入和发包验证不在当前纯净环境中执行。
- 任何超过 100MB 的输入仍必须使用流式、索引或分块读取，并完成进程和内存清理验收。

## 权威阅读顺序

1. 本文件：当前阶段和阻塞项摘要；
2. [`README.md`](README.md)：目录、已知结论和工具入口；
3. `analysis/` 中最新的专项报告：具体证据和结论；
4. [`WORK_RULES.md`](WORK_RULES.md)：详细执行规则；
5. 原始 dump 和资源：只读输入，不作为未经分析的结论。

## 完成状态

本目录同样采用：

```text
实现完成 → 验证通过 → 用户验收
```

验证必须绑定同一个提交号、输入文件版本、分析环境和证据。GPT 只能给出建议，用户完成最终验收。
